{
    "scripts": {
        "build": "rollup -c",
        "watch": "rollup -c -w",
        "dev": "npm-run-all --parallel start watch",
        "start": "serve public"
    },
    "dependencies": {
        "bmp-js": "^0.1.0",
        "canvas": "^2.6.1",
        "stackblur-canvas": "^2.2.0",
        "three": "^0.114.0"
    }
}
